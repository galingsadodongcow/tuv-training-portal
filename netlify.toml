[build]
  command = "next build"
  publish = ".next"

# Explicitly enable the Next.js runtime. Without this the build published no
# server functions, so the App Router app never ran and the live URL served
# stale content. The plugin wires up SSR/routing as Netlify Functions.
[[plugins]]
  package = "@netlify/plugin-nextjs"
